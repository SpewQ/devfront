services:
  juicy-check:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "5040:5040"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
